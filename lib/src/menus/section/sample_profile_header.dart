import 'package:flutter/material.dart';

class SampleProfileHeader extends StatelessWidget {
  const SampleProfileHeader({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(height: 180, width: double.maxFinite, color: Colors.grey);
  }
}
