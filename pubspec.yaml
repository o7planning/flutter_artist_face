name: flutter_artist_face
description: A standalone, highly customizable multi-platform Admin Dashboard layout framework for Flutter, featuring adaptive modular sidebars and unified topbar structures.
version: 0.9.4
resolution: workspace
homepage: https://o7planning.github.io

environment:
  sdk: ^3.10.7
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  shared_preferences: ^2.5.4
  just_the_tooltip: ^0.0.12
  url_launcher: ^6.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter: